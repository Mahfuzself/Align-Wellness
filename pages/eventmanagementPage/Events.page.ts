import { expect, Page } from "@playwright/test";
export default class EventsPage {
    private page: Page;
    // static login: any;
    constructor(page: Page) {
        this.page = page;
    }
    
 
}